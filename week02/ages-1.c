#include <stdio.h>
#include <cs50.h>

int main(void)
{
    //grab the ages of 4 students
    int n = 4;
    int age[n];
    // Do stuff
    for (int i = 0; i < n; i++)
    {
        age[i] = GetInt()
    }
}